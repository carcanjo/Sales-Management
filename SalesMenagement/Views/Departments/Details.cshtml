@model SalesMenagement.Models.Departments

@{
    ViewData["Title"] = "Details";
}

<h2>Details</h2>

<div>
    <h4>Departments</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.NameDepartaments)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.NameDepartaments)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning" ><i class="far fa-edit"></i> Edit</a> |
    <a asp-action="Index" class="btn btn-info"><i class="far fa-paper-plane"></i> Back to List</a>
</div>
